# Baked into the JS bundle at build time (Vite's own convention -- a static
# SPA build has no runtime env injection). Edit this to the real deployed API
# origin before building frontend/Dockerfile's `production` stage.
VITE_API_BASE_URL=https://api.yourdomain.com
